# 컬렉션 - 데이터를 수집
# list
# 변수를 여러 번 선언하지 않고, 여러 칸 list를 한 번만 선언하기 위해서 사용
# 변수를 선언하면, 값이 한 개만 담기고, 이름도 반드시 붙여야 한다.
# 여러 변수를 선언하면, 이름도 많아져서 관리하기 불편하다.
# 따라서, list는 이름이 하나고 각 값을 방 번호(인덱스)로 접근하기 때문에
# 값을 관리하기 훨씬 편하고 쉽다.

# 규칙성이 없는 값에 규칙성을 부여하기 위해서

# list = [값1, 값2, ...]
# list = [값] * 칸
# list = []
# list 길이 : len(list)